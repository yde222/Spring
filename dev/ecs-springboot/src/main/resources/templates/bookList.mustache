{{> layout/header}}

<!-- Page Header -->
<div class="text-center mb-16">
    <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-blue-500 to-purple-600 rounded-2xl mb-6 shadow-lg animate-float">
        <span class="text-3xl">📚</span>
    </div>
    <h1 class="text-5xl font-bold bg-gradient-to-r from-blue-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent mb-4">
        도서 컬렉션
    </h1>
    <p class="text-xl text-gray-600 mb-6 max-w-2xl mx-auto">
        엄선된 도서들을 만나보세요. 지식의 바다에서 새로운 발견을 시작하세요.
    </p>
    <div class="flex justify-center">
        <div class="w-32 h-1 bg-gradient-to-r from-blue-500 via-purple-500 to-indigo-500 rounded-full"></div>
    </div>
</div>

<!-- Statistics Bar -->
<div class="mb-12">
    <div class="bg-white/70 backdrop-blur-lg rounded-2xl p-6 shadow-lg border border-blue-100">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
            <div class="space-y-2">
                <div class="text-2xl font-bold text-blue-600">{{#bookList}}5{{/bookList}}{{^bookList}}0{{/bookList}}</div>
                <div class="text-sm text-gray-600">도서 보유</div>
            </div>
            <div class="space-y-2">
                <div class="text-2xl font-bold text-purple-600">24/7</div>
                <div class="text-sm text-gray-600">온라인 서비스</div>
            </div>
            <div class="space-y-2">
                <div class="text-2xl font-bold text-indigo-600">100%</div>
                <div class="text-sm text-gray-600">디지털화</div>
            </div>
        </div>
    </div>
</div>

<!-- Book Grid -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
    {{#bookList}}
    <div class="group bg-white/80 backdrop-blur-lg rounded-2xl shadow-lg hover:shadow-2xl border border-blue-100 overflow-hidden transform hover:-translate-y-3 transition-all duration-300">
        <!-- Book Header with Gradient -->
        <div class="relative bg-gradient-to-br from-blue-500 via-purple-500 to-indigo-600 p-6">
            <!-- Book Icon -->
            <div class="flex items-center justify-between mb-4">
                <div class="bg-white/20 backdrop-blur-sm rounded-full px-3 py-1">
                    <span class="text-white text-sm font-medium">#{{bookid}}</span>
                </div>
                <div class="text-white text-3xl group-hover:animate-bounce">📖</div>
            </div>
            
            <!-- Decorative Elements -->
            <div class="absolute top-0 right-0 w-20 h-20 bg-white/10 rounded-full -translate-y-10 translate-x-10"></div>
            <div class="absolute bottom-0 left-0 w-16 h-16 bg-white/10 rounded-full translate-y-8 -translate-x-8"></div>
        </div>
        
        <!-- Book Content -->
        <div class="p-6 space-y-4">
            <!-- Title -->
            <div>
                <h3 class="text-xl font-bold text-gray-900 line-clamp-2 group-hover:text-blue-600 transition-colors">
                    {{bookTitle}}
                </h3>
            </div>
            
            <!-- Author Info -->
            <div class="flex items-center space-x-2 text-gray-600">
                <div class="w-8 h-8 bg-gradient-to-r from-blue-400 to-purple-500 rounded-full flex items-center justify-center text-white text-sm">
                    👤
                </div>
                <span class="font-medium">{{author}}</span>
            </div>
            
            <!-- ISBN Badge -->
            <div class="flex items-center space-x-2">
                <span class="text-blue-500 text-sm font-medium">📋 ISBN:</span>
                {{#isbn}}
                <span class="font-mono text-sm bg-green-100 text-green-700 px-3 py-1 rounded-lg border border-green-200">등록됨</span>
                {{/isbn}}
                {{^isbn}}
                <span class="font-mono text-sm bg-gray-100 text-gray-500 px-3 py-1 rounded-lg border">미등록</span>
                {{/isbn}}
            </div>
            
            <!-- Price Section -->
            <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-xl p-4 border border-green-100">
                <div class="flex items-center justify-between">
                    <span class="text-gray-600 font-medium">정가</span>
                    <div class="text-right">
                        <span class="text-2xl font-bold text-green-600">₩{{price}}</span>
                        <div class="text-xs text-green-500">세금 포함</div>
                    </div>
                </div>
            </div>
            
            <!-- Action Buttons -->
            <div class="space-y-2 pt-2">
                <a href="/book/{{bookid}}" class="block w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white py-3 px-4 rounded-xl font-medium hover:from-blue-600 hover:to-purple-700 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl text-center">
                    <div class="flex items-center justify-center space-x-2">
                        <span>📖</span>
                        <span>상세 보기</span>
                    </div>
                </a>
                <button class="w-full bg-gray-100 text-gray-700 py-2 px-4 rounded-xl font-medium hover:bg-gray-200 transition-colors">
                    <div class="flex items-center justify-center space-x-2">
                        <span>❤️</span>
                        <span>찜하기</span>
                    </div>
                </button>
            </div>
        </div>
        
        <!-- Hover Overlay -->
        <div class="absolute inset-0 bg-gradient-to-t from-blue-600/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
    </div>
    {{/bookList}}
</div>

<!-- Empty State -->
{{^bookList}}
<div class="text-center py-20">
    <div class="inline-flex items-center justify-center w-32 h-32 bg-gradient-to-r from-gray-100 to-gray-200 rounded-full mb-8">
        <span class="text-6xl text-gray-400">📚</span>
    </div>
    <h3 class="text-2xl font-bold text-gray-700 mb-4">아직 등록된 도서가 없습니다</h3>
    <p class="text-gray-500 mb-8 max-w-md mx-auto">
        새로운 도서를 추가하여 라이브러리를 구축해보세요.
        첫 번째 책이 여러분을 기다리고 있습니다!
    </p>
    <a href="/book/0" class="inline-block bg-gradient-to-r from-blue-500 to-purple-600 text-white px-8 py-3 rounded-xl font-medium hover:from-blue-600 hover:to-purple-700 transform hover:scale-105 transition-all duration-200 shadow-lg">
        <div class="flex items-center space-x-2">
            <span>📖</span>
            <span>샘플 도서 보기</span>
        </div>
    </a>
</div>
{{/bookList}}

<!-- Call to Action Section -->
{{#bookList.0}}
<div class="mt-20 text-center">
    <div class="bg-white/70 backdrop-blur-lg rounded-2xl p-8 shadow-lg border border-blue-100">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">더 많은 도서를 찾고 계신가요?</h3>
        <p class="text-gray-600 mb-6">
            매주 새로운 도서가 추가됩니다. 알림을 설정하여 새로운 책을 놓치지 마세요!
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <button class="bg-gradient-to-r from-blue-500 to-purple-600 text-white px-6 py-3 rounded-xl font-medium hover:from-blue-600 hover:to-purple-700 transition-all duration-200 shadow-lg">
                📧 알림 설정
            </button>
            <button class="bg-gray-100 text-gray-700 px-6 py-3 rounded-xl font-medium hover:bg-gray-200 transition-colors">
                🔍 도서 검색
            </button>
        </div>
    </div>
</div>
{{/bookList.0}}

{{> layout/footer}}